//Source code generated by AppGPT (www.appgpt.tech)
import {
  Datagrid,
  List,
  EditButton,
  Edit,
  SimpleForm,
  Create,
  SelectColumnsButton,
  DatagridConfigurable,
  TopToolbar,
  CreateButton,
  ExportButton,
  FilterButton,
  //Field controls
  BooleanField,
  DateField,
  EmailField,
  ImageField,
  NumberField,
  ReferenceField,
  TextField,
  UrlField,
  //Input controls
  BooleanInput,
  DateInput,
  EmailInput,
  ImageInput,
  NumberInput,
  ReferenceInput,
  TextInput,
  UrlInput,
  PasswordInput,
} from 'react-admin';
import { useRecordContext } from 'react-admin';
import { Grid } from '@mui/material';
const ReadOnlyPasswordField = ({ record, source }) => {
  // You can customize the way you display the password here, e.g., mask it with asterisks
  const maskedPassword = '********';

  return <span>{maskedPassword}</span>;
};
const ListActions = () => (
  <TopToolbar>
    <FilterButton />
    <CreateButton />
    <ExportButton />
    <SelectColumnsButton />
  </TopToolbar>
);
const TransactionTitle = () => {
  const record = useRecordContext();
  return (
    <span>Transaction {record ? `"${record.transactionMethod}"` : ''}</span>
  );
};

export const TransactionList = () => (
  <List actions={<ListActions />} filters={ResourceFilters}>
    <DatagridConfigurable>
      <TextField source="transactionMethod" />
      <NumberField source="transactionAmount" />
      <DateField source="transactionDate" />
      <ReferenceField source="userId" reference="User" />
      <TextField source="transactionStatus" />
      <EditButton />
    </DatagridConfigurable>
  </List>
);

export const TransactionEdit = () => (
  <Edit title={<TransactionTitle />}>
    <SimpleForm>
      <Grid container rowSpacing={1} columnSpacing={{ xs: 1, sm: 2, md: 5 }}>
        <Grid item xs={4}>
          <TextInput source="transactionMethod" />
        </Grid>
        <Grid item xs={4}>
          <NumberInput source="transactionAmount" />
        </Grid>
        <Grid item xs={4}>
          <DateInput source="transactionDate" />
        </Grid>
        <Grid item xs={4}>
          <ReferenceInput source="userId" reference="User" />
        </Grid>
        <Grid item xs={4}>
          <TextInput source="transactionStatus" />
        </Grid>
      </Grid>
    </SimpleForm>
  </Edit>
);

export const TransactionCreate = () => (
  <Create>
    <SimpleForm>
      <Grid container rowSpacing={1} columnSpacing={{ xs: 1, sm: 2, md: 5 }}>
        <Grid item xs={4}>
          <TextInput source="transactionMethod" />
        </Grid>
        <Grid item xs={4}>
          <NumberInput source="transactionAmount" />
        </Grid>
        <Grid item xs={4}>
          <DateInput source="transactionDate" />
        </Grid>
        <Grid item xs={4}>
          <ReferenceInput source="userId" reference="User" />
        </Grid>
        <Grid item xs={4}>
          <TextInput source="transactionStatus" />
        </Grid>
      </Grid>
    </SimpleForm>
  </Create>
);

const ResourceFilters = [
  <TextInput source="q" label="Search" alwaysOn />,
  ,
  ,
  ,
  <ReferenceInput source="userId" label="userId" reference="User" alwaysOn />,
  ,
];
